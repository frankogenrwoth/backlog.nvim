-- lua/backlog/ui.lua

-- This module handles the UI components of the backlog plugin.
-- It includes the main task table, the side preview, and the input bar.

local M = {}

-- Function to open the backlog UI
function M.open()
  -- Logic to create the main floating window (task table)
  -- Logic to create the side preview window
  -- Logic to create the bottom input bar for comments/adding tasks
  print("Opening backlog UI")
end

-- Function to update the preview based on selected task
function M.update_preview(task_id)
  -- Logic to refresh the side preview pane
end

return M
